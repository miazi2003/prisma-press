enum ActiveStatus {
  ACTIVE
  BLOOCKED
}

enum Role {
  USER
  AUTHOR
  ADMIN
}

enum PostStatus {
  PUBLISHED
  ARCHIVED
  DRAFT
}

enum CommentStatus {
  APPROVED
  REJECT
}

enum SubscriptionStatus {
  ACTIVE
  CANCELED 
  EXPIRED
}
